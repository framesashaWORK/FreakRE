[package]
name = "dex-parser"
version = "0.1.0"
edition = "2021"
description = "Lightweight DEX (Dalvik Executable) parser for FreakRE"

[dependencies]
serde = { workspace = true }
adler = "1"
sha1 = "0.10"
