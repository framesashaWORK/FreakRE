[package]
name = "bibleteks-ir"
version = "0.1.0"
edition = "2021"
description = "Universal Intermediate Representation for FreakRE — architecture-agnostic SSA-based IR"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
hex = "0.4"

[dev-dependencies]
serde_json = "1.0"
