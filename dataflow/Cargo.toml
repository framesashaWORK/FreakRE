[package]
name = "dataflow"
version = "0.1.0"
edition = "2021"
description = "Data flow analysis for binary IR — reaching definitions, live variables, use-def chains"
license = "MIT"

[dependencies]
freakre-ir = { path = "../freakre-ir" }
thiserror = { workspace = true }
serde = { workspace = true }
indexmap = "2"

# [dev-dependencies]
# criterion = "0.5"

# [[bench]]
# name = "dataflow_bench"
# harness = false
