[package]
name = "freakre-dataflow"
version = "0.1.0"
edition = "2021"
description = "Data flow analysis for binary IR — reaching definitions, live variables, use-def chains"
license = "MIT"

[dependencies]
freakre-ir = { path = "../freakre-ir" }
thiserror = "1"
serde = { version = "1", features = ["derive"] }
indexmap = "2"

# [dev-dependencies]
# criterion = "0.5"

# [[bench]]
# name = "dataflow_bench"
# harness = false
