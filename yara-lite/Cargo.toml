[package]
name = "yara-lite"
version = "0.1.0"
edition = "2021"
description = "Lightweight YARA-compatible pattern matching engine for malware analysis"

[dependencies]
aho-corasick = "1"
regex = "1"
thiserror = "2"

[dev-dependencies]
