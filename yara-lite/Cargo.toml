[package]
name = "yara-lite"
version = "0.1.0"
edition = "2021"
description = "Lightweight YARA-compatible pattern matching engine for malware analysis"

[dependencies]
freakre-patterns = { path = "../freakre-patterns" }
thiserror = { workspace = true }

[dev-dependencies]
