[package]
name = "dotnet-analyzer"
version = "0.1.0"
edition = "2021"
description = ".NET / C# assembly analyzer for FreakRE (CLR header, #~ stream, managed metadata)"

[dependencies]
serde = { workspace = true }
