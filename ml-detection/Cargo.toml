[package]
name = "ml-detection"
version = "0.1.0"
edition = "2021"
description = "ML-based malware classification — feature extraction + heuristic ensemble classifier"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
