[package]
name = "ml-detection"
version = "0.1.0"
edition = "2021"
description = "Heuristic ensemble malware classification — feature extraction + hand-tuned weighted decision trees"
license = "MIT"

[dependencies]
serde = { workspace = true }
freakre-config = { path = "../freakre-config" }
thiserror = { workspace = true }
