[package]
name = "capstone-ffi"
version = "0.1.0"
edition = "2021"
description = "Capstone disassembly framework FFI bindings with multi-architecture support"
license = "MIT"

[dependencies]
freakre-x86 = { path = "../freakre-x86" }
thiserror = { workspace = true }
serde = { workspace = true }

[build-dependencies]
pkg-config = "0.3"

# [dev-dependencies]
# criterion = "0.5"

# [[bench]]
# name = "disasm_bench"
# harness = false
