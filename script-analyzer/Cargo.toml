[package]
name = "script-analyzer"
version = "0.1.0"
edition = "2021"
description = "Script language analyzer (PowerShell, AutoIt, AutoHotkey, BAT, VBS) for FreakRE"

[dependencies]
serde = { workspace = true }
